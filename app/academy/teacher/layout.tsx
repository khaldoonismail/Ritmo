import TeacherShell from "@/components/teacher/TeacherShell";

export default function AcademyTeacherLayout({ children }: { children: React.ReactNode }) {
  return <TeacherShell>{children}</TeacherShell>;
}
